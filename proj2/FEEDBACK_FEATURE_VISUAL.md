# AI Feedback Loop Feature - Visual Flow

## 🎯 Feature Overview
Users can now provide feedback (Like/Dislike) on recommendations, and the system learns from this feedback to improve future recommendations.

## 🔄 User Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    USER VIEWS RECOMMENDATIONS                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Meal Card 1 │  │  Meal Card 2 │  │  Meal Card 3 │      │
│  │              │  │              │  │              │      │
│  │  [👍 Like]   │  │  [👍 Like]   │  │  [👍 Like]   │      │
│  │  [👎 Dislike]│  │  [👎 Dislike]│  │  [👎 Dislike]│      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                          │
                          │ User clicks Like/Dislike
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              FRONTEND: RecommendationCarousel.tsx            │
│  • handleFeedback(itemId, 'like'/'dislike')                 │
│  • Calls recommendationApi.submitFeedback()                  │
└─────────────────────────────────────────────────────────────┘
                          │
                          │ POST /api/recommend/feedback
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              BACKEND: /api/recommend/feedback                │
│  • Router: recommend.py                                      │
│  • Service: FeedbackService.submit_feedback()                │
│  • Stores in: recommendation_feedback table                  │
└─────────────────────────────────────────────────────────────┘
                          │
                          │ Feedback stored
                          ▼
┌─────────────────────────────────────────────────────────────┐
│         NEXT TIME: User requests recommendations             │
│                                                              │
│  RecommendationEngine.get_meal_recommendations()           │
│  ┌────────────────────────────────────────────────────┐    │
│  │ 1. Get all candidates                              │    │
│  │ 2. Apply safety filters (allergies, diets)        │    │
│  │ 3. Filter out DISLIKED items ❌                    │    │
│  │ 4. Rank remaining items                           │    │
│  │ 5. Boost scores for LIKED items ⬆️ (+10%)          │    │
│  │ 6. Return top recommendations                      │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              USER SEES IMPROVED RECOMMENDATIONS              │
│  • No disliked items shown                                  │
│  • Liked items appear with higher scores                    │
│  • System learns and adapts over time                       │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Database Schema

```
┌─────────────────────────────────────────┐
│     recommendation_feedback              │
├─────────────────────────────────────────┤
│ id (PK)                                  │
│ user_id (FK → users.id)                  │
│ item_id (menu_item_id or restaurant_id)  │
│ item_type ('meal' | 'restaurant')        │
│ feedback_type ('like' | 'dislike')       │
│ notes (optional)                          │
│ created_at                               │
│ updated_at                               │
└─────────────────────────────────────────┘
```

## 🎨 UI Changes

### Before:
```
┌─────────────────────────────┐
│  Recommended Meal           │
│  Score: 85%                 │
│  Explanation: ...           │
│  [Reasoned by LLM engine]   │
└─────────────────────────────┘
```

### After:
```
┌─────────────────────────────┐
│  Recommended Meal           │
│  Score: 85%                 │
│  Explanation: ...           │
│  [Reasoned by LLM engine]   │
│                             │
│  ┌─────────┐  ┌──────────┐ │
│  │ 👍 Like │  │ 👎 Dislike│ │
│  └─────────┘  └──────────┘ │
└─────────────────────────────┘
```

## 🔧 Technical Implementation

### Backend Architecture
```
models.py
  └── RecommendationFeedbackDB (new model)
       │
       ├── feedback_service.py (new service)
       │    ├── submit_feedback()
       │    ├── get_user_disliked_items()
       │    └── get_user_liked_items()
       │
       ├── engine.py (updated)
       │    ├── get_meal_recommendations() [filters disliked]
       │    ├── get_restaurant_recommendations() [filters disliked]
       │    └── _apply_feedback_boosts() [boosts liked]
       │
       └── recommend.py (router)
            └── POST /api/recommend/feedback (new endpoint)
```

### Frontend Architecture
```
api.ts
  └── recommendationApi.submitFeedback() (new method)
       │
       └── RecommendationCarousel.tsx (updated)
            ├── handleFeedback() (new handler)
            ├── feedbackLoading state (new state)
            └── Like/Dislike buttons (new UI)
```

## 📈 Impact

- **User Experience**: Users can now influence their recommendations
- **Personalization**: System learns from user preferences
- **Quality**: Disliked items are filtered out automatically
- **Engagement**: Liked items get priority in future recommendations

